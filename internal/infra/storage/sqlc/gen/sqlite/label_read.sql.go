// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: label_read.sql

package sqlitegen

import (
	"context"
)

const getLabelListView = `-- name: GetLabelListView :many

SELECT l.id, l.user_id, l.name, l.icon, l.position, l.is_archived, l.created_at, l.updated_at
FROM labels l
WHERE l.user_id = ?
   OR l.user_id IN (
       SELECT a.user_id
       FROM accounts_access aa
       JOIN accounts a ON a.id = aa.account_id
       WHERE aa.user_id = ? AND aa.is_accepted = 1
   )
ORDER BY l.position, l.id
`

type GetLabelListViewParams struct {
	UserID   string
	UserID_2 string
}

// Read-model query for the label module (CQRS read side).
// Available labels: the user's OWN labels plus the labels of every user who has
// shared an account WITH this user. The user id is repeated positionally, which
// generates a two-field Params struct.
func (q *Queries) GetLabelListView(ctx context.Context, arg GetLabelListViewParams) ([]Label, error) {
	rows, err := q.db.QueryContext(ctx, getLabelListView, arg.UserID, arg.UserID_2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Label{}
	for rows.Next() {
		var i Label
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Icon,
			&i.Position,
			&i.IsArchived,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
