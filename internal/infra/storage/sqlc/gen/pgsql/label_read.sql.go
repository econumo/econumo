// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: label_read.sql

package pgsqlgen

import (
	"context"
)

const getLabelListView = `-- name: GetLabelListView :many

SELECT l.id, l.user_id, l.name, l.icon, l.sort_key, l.is_archived, l.created_at, l.updated_at
FROM labels l
WHERE l.user_id = $1
   OR l.user_id IN (
       SELECT a.user_id
       FROM accounts_access aa
       JOIN accounts a ON a.id = aa.account_id
       WHERE aa.user_id = $1 AND aa.is_accepted = true
   )
ORDER BY l.sort_key, l.id
`

// Read-model query for the label module (PostgreSQL variant: $N placeholders).
// See the sqlite variant for documentation.
// Available labels: own + labels of users who shared an account with this user.
// $1 is reused for both positions so the generated param stays single.
func (q *Queries) GetLabelListView(ctx context.Context, userID string) ([]Label, error) {
	rows, err := q.db.QueryContext(ctx, getLabelListView, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Label{}
	for rows.Next() {
		var i Label
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Icon,
			&i.SortKey,
			&i.IsArchived,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
