// The Vue Home renders Budget (onboarded) or Onboarding. Both arrive in later
// plans; until then the workspace at "/" stays empty (the compact layout shows
// the sidebar pane instead).
export function HomePage() {
  return <div data-testid="home-placeholder" />
}
