.secret*
.idea
.DS_Store

# Runtime data (local sqlite db, logs, cache)
/var/

# Local env overrides
/.env.local*

# Local-only PHP↔Go comparison tooling (kept out of the repo)
deployment/compare/

# Local dev-machine -> homelab transfer script (contains the homelab host/path).
/sync-to-homelab.sh
